import { ethers } from 'ethers';

// Simplified contract types - just use any for now to avoid complex type issues
export type StablecoinContract = ethers.Contract;
export type KYCRegistryContract = ethers.Contract;
